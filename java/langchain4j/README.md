# LangChain4j + Ollama

Maven 本地仓库：`java/.m2`（通过 `java/settings.xml`）。

```powershell
cd f:\commercial\llm-agents\java\langchain4j
mvn -Dmaven.repo.local=..\.m2\repository -q exec:java "-Dexec.args=测试主题"
```
