# AG2 (pyautogen)

## 范式

多 Agent 对话（UserProxy + Assistant），适合研究式协作。

## Ollama

`config_list` 中 `api_type: openai` + `base_url` 指向 Ollama。

## 注意

原版 AutoGen 已进入维护；社区 AG2 延续 v0.2 对话模型。与 Microsoft Agent Framework 对照阅读。
