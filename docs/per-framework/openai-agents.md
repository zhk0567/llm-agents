# OpenAI Agents SDK (TypeScript)

## 范式

`Agent` + `tool` + `run()`，Handoff 扩展多 Agent。

## Ollama

`OpenAI` 客户端 `baseURL` 指向 `http://127.0.0.1:11434/v1`。

## 何时选用

熟悉 OpenAI Agents API 的全栈 TS 团队。
