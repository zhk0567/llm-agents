# LangGraph.js

## 范式

与 Python 版相同的 ReAct Agent（`@langchain/langgraph/prebuilt`）。

## 运行

```powershell
cd typescript\langgraph-js
npm install
npm run start -- "主题"
```

## Ollama

`ChatOllama` + `config/ollama.json`。
