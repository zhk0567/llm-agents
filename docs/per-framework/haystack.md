# Haystack 2.x

## 范式

`haystack.components.agents.Agent` + `Tool` + `OllamaChatGenerator`。

## Ollama

`ollama-haystack` 集成，`url` 指向 `config/ollama.json` 的 `host`。

## 何时选用

已有 Haystack 管道 / RAG 栈，希望 Agent 与 Pipeline 统一。
