# TypeScript Agent 示例

工作区包含 `langgraph-js` 与 `openai-agents`。依赖安装在 `typescript/node_modules`。

```powershell
cd f:\commercial\llm-agents\typescript
npm install
cd langgraph-js
npm run start -- "测试主题"
```
